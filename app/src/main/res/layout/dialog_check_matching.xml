<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="dialog_check_matching"
            type="android.app.Dialog" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_check_matching"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/img_check_matching"
            android:layout_width="65dp"
            android:layout_height="88dp"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="10dp"
            app:srcCompat="@drawable/ic_check_image" />

        <TextView
            android:id="@+id/lb_check_matching"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="제목을 입력하고 참가조건을 설정해 주세요."
            android:textColor="@color/black"
            android:textSize="15sp" />

        <Button
            android:id="@+id/btn_check_matching"
            android:layout_width="296dp"
            android:layout_height="50dp"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:layout_marginLeft="16dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="16dp"
            android:layout_marginRight="16dp"
            android:layout_marginBottom="20dp"
            android:background="@color/floating_background"
            android:text="확인"
            android:textSize="15sp"
            app:backgroundTint="@null" />
    </LinearLayout>
</layout>